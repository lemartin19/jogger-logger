{
  "name": "jogger-logger-server",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "./.secrets.conf && npx ts-node ./src/index.ts",
    "ts": "tsc --noEmit -w",
    "format": "prettier --write src/"
  },
  "devDependencies": {
    "@types/node": "^19.6.0"
  }
}
